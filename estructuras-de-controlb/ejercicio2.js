// Patrón

// Crear un programa que muestre en consola el siguiente patrón

const patron = [];
let repeticiones = "6";

for (let i = 0; i < repeticiones; i++) {
  patron.push("*");
  console.log(patron)
}
